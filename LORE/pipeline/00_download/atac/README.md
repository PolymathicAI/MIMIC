# ATAC-seq Peak Download (ENCODE, narrowPeak)

This module downloads **ATAC-seq peak files** from ENCODE for multiple organisms (currently human and mouse, plus any others that match the ENCODE query) and builds a **metadata table** for downstream processing.

To run this code: `python -m data-processing.00_download.atac.00_download_atac`